/** Post composer limits (shared between composer UI + client-side validation). */
export const MAX_POST_LENGTH = 1800;       // ≈ 300 words
export const POST_WARN_AT = 1600;          // remaining-chars warning threshold
export const MAX_IMAGES_PER_POST = 4;
